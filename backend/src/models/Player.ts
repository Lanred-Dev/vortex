/**
 * Represents a player in the game. This is a simplified version of the User model, containing only the information relevant to the game state.
 */

export interface Player {
    id: string;
    color: string;
}